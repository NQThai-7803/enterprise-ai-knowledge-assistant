# Database Design

## 1. Principles

- PostgreSQL is the primary database.
- All schema changes are managed by Alembic migrations.
- Entity primary keys use UUID values. Newer tables and TASK-022 `audit_logs.id` use PostgreSQL `gen_random_uuid()` server defaults; older tables may still use application-side UUID defaults until a future cleanup task.
- Timestamp columns use timezone-aware PostgreSQL `timestamptz` values and are stored as UTC instants.
- Passwords are never stored in plaintext.
- Raw refresh tokens are never stored in the database.

## 2. Implemented schema as of TASK-008

### user_role enum

PostgreSQL enum name: `user_role`

Values:

```text
ADMIN
MANAGER
STAFF
```

### departments

| Column | Type | Notes |
|---|---|---|
| id | UUID | Primary key, generated by the application |
| name | varchar(120) | Required, unique |
| code | varchar(50) | Required, unique |
| description | text | Nullable |
| created_at | timestamptz | Required, server default `now()` |

Constraints:

- `pk_departments`
- `uq_departments_name`
- `uq_departments_code`

### users

| Column | Type | Notes |
|---|---|---|
| id | UUID | Primary key, generated by the application |
| email | varchar(320) | Required, unique |
| full_name | varchar(200) | Required |
| hashed_password | varchar(255) | Required Argon2 password hash only |
| role | user_role | Required |
| department_id | UUID | Nullable FK to `departments.id` |
| is_active | boolean | Required, Python default true, server default true |
| created_at | timestamptz | Required, server default `now()` |
| updated_at | timestamptz | Required, server default `now()`, updated by ORM on update |

Constraints and indexes:

- `pk_users`
- `uq_users_email`
- `fk_users_department_id_departments`
- `ix_users_department_id`

Foreign key behavior:

```text
users.department_id -> departments.id ON DELETE SET NULL
```

`users.department_id` is intentionally nullable. Admin users may have no department. Manager and Staff department requirements will be validated in later service/API tasks.

### refresh_tokens

| Column | Type | Notes |
|---|---|---|
| id | UUID | Primary key, generated by the application |
| user_id | UUID | Required FK to `users.id` |
| token_hash | varchar(64) | Required, unique SHA-256 hex hash of the raw refresh token |
| expires_at | timestamptz | Required |
| revoked_at | timestamptz | Nullable |
| created_at | timestamptz | Required, server default `now()` |

Constraints and indexes:

- `pk_refresh_tokens`
- `uq_refresh_tokens_token_hash`
- `fk_refresh_tokens_user_id_users`
- `ix_refresh_tokens_user_id`
- `ix_refresh_tokens_expires_at`

Foreign key behavior:

```text
refresh_tokens.user_id -> users.id ON DELETE CASCADE
```

Refresh token rules:

- Raw refresh tokens are opaque random values returned only at login or refresh.
- Only `token_hash` is stored.
- `token_hash` is `hashlib.sha256(token.encode("utf-8")).hexdigest()`.
- Refresh rotates tokens: the old row receives `revoked_at`, and a new row is created.
- Token family, IP address, and user agent are not stored in TASK-005.

## 3. Alembic-managed tables

After TASK-008, a migrated local database should contain these project tables:

```text
alembic_version
departments
users
refresh_tokens
audit_logs
documents
document_permissions
```

No PostgreSQL extension is created in TASK-008. The `document_chunks` table, pgvector extension, vector columns, and vector indexes are not implemented yet.

## 4. Password storage

Only `users.hashed_password` is stored. Plaintext passwords are accepted only by command/runtime input, hashed with `pwdlib` Argon2 helpers, and never persisted in plaintext.
## 5. TASK-007/TASK-022 administration and audit schema

### audit_logs

TASK-007 introduced the `audit_logs` table. TASK-022 expands the same table; no duplicate audit table is created.

| Column | Type | Notes |
|---|---|---|
| id | UUID | Primary key, server default `gen_random_uuid()` after TASK-022 |
| user_id | UUID | Nullable actor FK to `users.id`; `NULL` for anonymous/system events |
| action | varchar(100) | Required stable event type; maps to API `event_type` |
| outcome | varchar(16) | Required `SUCCESS` or `FAILURE`; default `SUCCESS` for legacy rows |
| entity_type | varchar(64) | Nullable safe target type such as `USER`, `DOCUMENT`, `CHAT_MESSAGE`, or `FEEDBACK` |
| entity_id | UUID | Nullable target UUID |
| request_id | varchar(128) | Nullable request correlation id |
| error_code | varchar(100) | Nullable sanitized failure code |
| ip_address | inet | Nullable direct client IP; proxy headers are not trusted by TASK-022 |
| user_agent | text | Nullable, length-limited before persistence |
| metadata | jsonb | Required, defaults to `{}`; sanitized allowlisted scalar metadata only |
| created_at | timestamptz | Required, server default `now()` |

Foreign key behavior:

```text
audit_logs.user_id -> users.id ON DELETE SET NULL
```

Indexes:

- `ix_audit_logs_action`
- `ix_audit_logs_created_at`
- `ix_audit_logs_user_id`
- `ix_audit_logs_created_id`
- `ix_audit_logs_action_created`
- `ix_audit_logs_user_created`
- `ix_audit_logs_entity_created`
- `ix_audit_logs_outcome_created`

Implemented event types after TASK-022:

- Authentication: `AUTH_LOGIN_SUCCEEDED`, `AUTH_LOGIN_FAILED`.
- Administration: `USER_CREATED`, `USER_UPDATED`, `USER_STATUS_CHANGED`, `DEPARTMENT_CREATED`, `DEPARTMENT_UPDATED`, `DEPARTMENT_DELETED`.
- Documents: `DOCUMENT_UPLOADED`, `DOCUMENT_UPDATED`, `DOCUMENT_DOWNLOADED`, `DOCUMENT_DELETED`, `DOCUMENT_PERMISSION_GRANTED`, `DOCUMENT_PERMISSION_UPDATED`, `DOCUMENT_PERMISSION_REVOKED`.
- Chat: `CHAT_SESSION_CREATED`, `CHAT_QUESTION_SUBMITTED`, `CHAT_ANSWER_GENERATED`, `CHAT_NO_ANSWER_GENERATED`.
- Feedback: `FEEDBACK_UPSERTED`.

Append-only policy:

- Application code exposes only create/list/count repository operations for AuditLog.
- There is no public create, update, or delete AuditLog API.
- Audit report reads do not insert recursive audit events.
- Database administrators can still alter rows directly; cryptographic ledger, signing, external immutable storage, retention cleanup, export, and SIEM integration are out of scope for the MVP.

Audit metadata allowlist:

- Allowed keys include role/status fields, scope/permission fields, UUID identifier fields, counts, rating, grounding status, processing outcome, and `actor_type` for system events.
- Values are scalars only: string enum values, non-negative integer counts, booleans, UUID strings, or null.
- Nested objects, arrays, bytes, ORM instances, exceptions, secret values, overlong strings, and unknown keys are rejected.
- Metadata must not store passwords, password hashes, access tokens, refresh tokens, token hashes, authorization headers, email, full name, Chat question/answer text, prompts, retrieved context, citation excerpts, Feedback reason, Document title, filename, storage key, local path, provider error body, SQL details, or stack traces.

Audit transaction rule:

- Successful User, Department, Document, DocumentPermission, Chat message, ChatSession, and Feedback mutations record success audit rows in the same PostgreSQL transaction as the business mutation.
- If required success audit insertion fails, the business mutation rolls back and the API does not return a false success.
- Failure events use a best-effort separate transaction after the business transaction is rolled back; if failure-audit insertion fails, the original error is preserved.

## 6. TASK-008 document schema

TASK-008 implements the document metadata and explicit permission grant data model only. It does not implement upload, local storage, document APIs, PDF processing, chunks, embeddings, pgvector, or retrieval policies.

### document_status enum

PostgreSQL enum name: `document_status`

Values:

```text
UPLOADED
PROCESSING
READY
FAILED
ARCHIVED
```

`is_deleted` is a separate soft-delete flag and is not represented as a `DocumentStatus` value.

### document_access_scope enum

PostgreSQL enum name: `document_access_scope`

Values:

```text
PRIVATE
DEPARTMENT
ORGANIZATION
```

Scope consistency is protected by database constraint `ck_documents_access_scope_department`:

- `DEPARTMENT` requires `department_id IS NOT NULL`.
- `PRIVATE` and `ORGANIZATION` require `department_id IS NULL`.

### document_permission_level enum

PostgreSQL enum name: `document_permission_level`

Values:

```text
VIEW
EDIT
MANAGE
```

The intended permission order `VIEW < EDIT < MANAGE` is not implemented by string comparison. Explicit mapping belongs to the future document access policy task.

### documents

| Column | Type | Notes |
|---|---|---|
| id | UUID | Primary key, generated by the application |
| title | varchar(255) | Required |
| description | text | Nullable |
| original_filename | varchar(255) | Required display name only; not used as a storage path |
| storage_key | varchar(512) | Required internal storage identifier, unique, not a public URL |
| mime_type | varchar(127) | Required, not hard-coded to PDF at database level |
| file_size | bigint | Required, must be greater than 0 |
| checksum_sha256 | varchar(64) | Required, indexed, lowercase SHA-256 hex checksum of file bytes |
| status | document_status | Required, defaults to `UPLOADED` |
| access_scope | document_access_scope | Required, defaults to `PRIVATE` |
| department_id | UUID | Nullable FK to `departments.id`; required only for `DEPARTMENT` scope |
| uploaded_by | UUID | Required FK to `users.id` |
| error_message | text | Nullable sanitized processing error message |
| is_deleted | boolean | Required, defaults to false |
| created_at | timestamptz | Required, server default `now()` |
| updated_at | timestamptz | Required, server default `now()`, updated by ORM on update |

Constraints and indexes:

- `pk_documents`
- `uq_documents_storage_key`
- `ck_documents_file_size_positive`
- `ck_documents_checksum_sha256_length`
- `ck_documents_checksum_sha256_lower_hex`
- `ck_documents_access_scope_department`
- `ix_documents_checksum_sha256`
- `ix_documents_uploaded_by`
- `ix_documents_status_is_deleted`
- `ix_documents_department_id_access_scope_is_deleted`
- `ix_documents_created_at`

Foreign key behavior:

```text
documents.department_id -> departments.id ON DELETE RESTRICT
documents.uploaded_by -> users.id ON DELETE RESTRICT
```

A Department referenced by a department-scoped Document cannot be hard deleted. Department service currently does not map this document-reference conflict to a dedicated business error code.

`checksum_sha256` is not unique in TASK-008. Duplicate file policy belongs to the upload task.

Document delete is intended to be soft delete by setting `is_deleted = true`. `ARCHIVED` is a business status and is distinct from soft delete.

### document_permissions

| Column | Type | Notes |
|---|---|---|
| id | UUID | Primary key, generated by the application |
| document_id | UUID | Required FK to `documents.id` |
| user_id | UUID | Nullable FK to `users.id` |
| department_id | UUID | Nullable FK to `departments.id` |
| permission | document_permission_level | Required, defaults to `VIEW` |
| created_by | UUID | Required FK to `users.id` actor |
| created_at | timestamptz | Required, server default `now()` |

Constraints and indexes:

- `pk_document_permissions`
- `ck_document_permissions_exactly_one_grantee`
- `uq_document_permissions_document_user`
- `uq_document_permissions_document_department`
- `ix_document_permissions_document_id`
- `ix_document_permissions_user_id`
- `ix_document_permissions_department_id`
- `ix_document_permissions_created_by`

Foreign key behavior:

```text
document_permissions.document_id -> documents.id ON DELETE CASCADE
document_permissions.user_id -> users.id ON DELETE CASCADE
document_permissions.department_id -> departments.id ON DELETE CASCADE
document_permissions.created_by -> users.id ON DELETE RESTRICT
```

A permission grant must have exactly one grantee: either one User or one Department. Duplicate grants for the same `(document_id, user_id)` or `(document_id, department_id)` are rejected. Permission rows are explicit allow grants only; explicit deny is not supported.

Direct grants supplement access from `access_scope`; they do not reduce access granted by `PRIVATE`, `DEPARTMENT`, or `ORGANIZATION` semantics. Admin implicit access and uploader ownership are not stored as permission rows.
## 7. TASK-010 document access behavior

TASK-010 does not change the database schema and does not add a migration. It implements permission-aware queries over the TASK-008 tables.

Document access rules now implemented in repository/service code:

- List, detail, status, and download apply `documents.is_deleted = false` in PostgreSQL.
- Admin access uses the same soft-delete filter.
- Non-Admin access is an `OR` over Organization scope, same Department scope, uploader ownership, direct User grant, and direct Department grant.
- Direct grant checks use correlated `EXISTS` expressions to avoid duplicate Document rows.
- List `total` counts only accessible Documents after applying the same permission filter and query filters.
- Search is parameterized over `title`, `original_filename`, and `description`.
- Sort fields are allowlisted and include a secondary `documents.id` sort for stable results.

Document deletion remains soft delete by setting `documents.is_deleted = true`. Direct grants remain in `document_permissions` after soft delete, but they do not bypass deleted state.

Department hard delete now checks all Documents referencing `documents.department_id`, including soft-deleted rows, and returns `DEPARTMENT_HAS_DOCUMENTS` before attempting the delete. Direct Department grants still use `ON DELETE CASCADE` and do not block Department deletion.

Permission levels are evaluated through explicit mappings for view, edit, and manage. There is no explicit deny grant.

Before TASK-014, `document_chunks`, pgvector extension, vector columns, vector indexes, embeddings, and RAG retrieval were not implemented. TASK-014 now adds chunk vector storage; permission-aware retrieval and RAG APIs remain future work.

## 8. TASK-014 document_chunks and pgvector schema

TASK-014 adds PostgreSQL pgvector storage for processed chunks.

### document_chunks

| Column | Type | Notes |
|---|---|---|
| id | UUID | Primary key, generated by the application |
| document_id | UUID | Required FK to `documents.id` |
| chunk_index | integer | Required, zero-based per Document |
| text | text | Required original chunk text |
| token_count | integer | Required, greater than 0 |
| character_count | integer | Required, greater than 0 |
| page_numbers | integer[] | Required source pages |
| start_page | integer | Required, greater than 0 |
| end_page | integer | Required, `>= start_page` |
| overlap_token_count | integer | Required, `>= 0` |
| content_sha256 | varchar(64) | Required deterministic SHA-256 of chunk text |
| embedding | vector(384) | Required dense embedding |
| embedding_provider | varchar(64) | Required provider id |
| embedding_model | varchar(255) | Required model name |
| embedding_dimensions | integer | Required, fixed to 384 |
| created_at | timestamptz | Required, server default `now()` |

Constraints and indexes:

- `document_chunks.document_id -> documents.id ON DELETE CASCADE`.
- Unique `(document_id, chunk_index)` via `uq_document_chunks_document_chunk_index`.
- Check constraints protect positive counts, page range, checksum length, non-empty page array, and dimension 384.
- Indexes exist on `document_id`, `(document_id, chunk_index)`, and `content_sha256`.
- HNSW cosine index `ix_document_chunks_embedding_hnsw_cosine` uses `vector_cosine_ops`.

Soft-deleted Documents may still have chunk rows. Future retrieval must join to `documents` and filter `documents.is_deleted = false`, status, and permissions. Permission-aware semantic retrieval is not implemented yet.

Chunk replacement is atomic through repository/service code: new payload is validated before old chunks are deleted, and the repository does not commit. The service owns commit/rollback.

The migration enables `CREATE EXTENSION IF NOT EXISTS vector`. Downgrade drops `document_chunks` and indexes but intentionally does not drop the `vector` extension because it may predate this migration.

## 10. TASK-017 full-text retrieval index

TASK-017 adds one index for keyword retrieval:

```sql
CREATE INDEX ix_document_chunks_text_fts_simple
ON document_chunks
USING gin (
    to_tsvector('simple'::regconfig, text)
);
```

- The index is an expression GIN index; no stored `tsvector` column is added.
- Keyword ranking is computed at query time with `ts_rank_cd(...)` and does not require schema columns.
- The existing `ix_document_chunks_embedding_hnsw_cosine` pgvector HNSW index remains in place for semantic retrieval.
- No search-history table, retrieval-cache table, conversation table, citation table, trigger, `unaccent`, `pg_trgm`, or new extension is introduced.
- The TASK-017 downgrade removes only `ix_document_chunks_text_fts_simple`; it does not drop `document_chunks`, vector columns, the pgvector extension, documents, permissions, or the HNSW vector index.

## TASK-018/TASK-019 Chat tables

Chat persistence uses `chat_sessions` and `chat_messages`.

- ChatSession rows are owned by one User.
- ChatMessage rows belong to a ChatSession and store role, content, optional retrieval query, provider response time, prompt tokens, and completion tokens.
- SYSTEM messages are internal and hidden from public Chat history.
- USER and ASSISTANT answer pairs are persisted atomically after retrieval and LLM generation complete.

## TASK-020 `message_citations`

`message_citations` stores validated source metadata for ASSISTANT messages.

| Field | Notes |
| --- | --- |
| `id` | UUID primary key |
| `message_id` | Required FK to `chat_messages.id`, `ON DELETE CASCADE` |
| `document_id` | Required FK to `documents.id`, `ON DELETE RESTRICT` |
| `chunk_id` | Nullable FK to `document_chunks.id`, `ON DELETE SET NULL` |
| `page_number` | Required integer, `> 0` |
| `excerpt` | Required nonblank server-generated text snapshot |
| `relevance_score` | Nullable numeric, `0 <= value <= 1`; semantic score only |
| `citation_order` | Required integer, starts at 1 |

Constraints and indexes:

- `ck_message_citations_order_positive`: `citation_order >= 1`.
- `ck_message_citations_page_positive`: `page_number > 0`.
- `ck_message_citations_excerpt_not_blank`: `char_length(btrim(excerpt)) > 0`.
- `ck_message_citations_relevance_score_range`: nullable score in `[0, 1]`.
- `uq_message_citations_message_order`: unique `(message_id, citation_order)`.
- `ix_message_citations_message_order`: `(message_id, citation_order)`.
- `ix_message_citations_document`: `(document_id)`.

Citation rules:

- Citations attach only to ASSISTANT messages; the service enforces this because role is cross-table state.
- USER and SYSTEM messages have no citations.
- Excerpt is a server snapshot from the selected chunk, not LLM-provided text.
- Document title is read from the current Document row when building API responses.
- `chunk_id` is nullable so historical citations do not block future chunk replacement or reprocessing.
- No embeddings, storage keys, permission rows, session duplicate, user duplicate, confidence enum, or arbitrary metadata JSON are stored.

## TASK-021 `feedback`

`feedback` stores one current rating per User per ASSISTANT message.

| Field | Notes |
| --- | --- |
| `id` | UUID primary key, server generated |
| `message_id` | Required FK to `chat_messages.id`, `ON DELETE CASCADE` |
| `user_id` | Required FK to `users.id`, `ON DELETE RESTRICT` |
| `rating` | Required `feedback_rating` enum: `HELPFUL` or `NOT_HELPFUL` |
| `reason` | Nullable text, blank values normalized to `NULL` by the service |
| `created_at` | Required timestamptz, server default `now()` |
| `updated_at` | Required timestamptz, server default `now()`, updated on upsert conflict |

Constraints and indexes:

- `uq_feedback_message_user`: unique `(message_id, user_id)`.
- `ck_feedback_reason_not_blank`: `reason IS NULL OR char_length(btrim(reason)) > 0`.
- `ck_feedback_reason_max_length`: `reason IS NULL OR char_length(reason) <= 1000`.
- `ix_feedback_user_updated`: `(user_id, updated_at)`.
- `ix_feedback_rating_updated`: `(rating, updated_at)`.
- `ix_feedback_message`: `(message_id)`.

Feedback rules:

- Feedback is valid only for ASSISTANT messages; the service enforces this with an owned target SQL lookup because message role is cross-table state.
- Feedback does not store email, Department, role, answer snapshots, questions, citations, Document IDs, or arbitrary metadata JSON.
- Hard-deleting an ASSISTANT message cascades its feedback.
- User deletion is restricted when feedback exists; user deactivation does not delete historical feedback.

## TASK-034.1.2 exact evidence citation metadata

A manual citation-quality hardening follow-up adds exact evidence metadata to `message_citations`.

### `message_citations.evidence_text`

| Field | Notes |
| --- | --- |
| `evidence_text` | Nullable `text`; focused backend-generated evidence associated with the validated citation |

Constraint:

```text
ck_message_citations_evidence_text_not_blank
evidence_text IS NULL OR char_length(btrim(evidence_text)) > 0
```

Migration:

```text
cd124fa71c86_add_citation_evidence_text.py
down_revision = 20260803_0010
```

Operational note:

- The revision file was initially created with empty `upgrade()`/`downgrade()` bodies and was accidentally stamped as current.
- The migration was corrected, the database was stamped back to `20260803_0010`, and the corrected migration was upgraded again.
- Runtime verification confirmed the physical PostgreSQL column and successful persistence.

Data rules:

- Historical citation rows may remain `NULL`; no backfill is required.
- `excerpt` remains the broader evidence/context snapshot.
- `evidence_text` is additional presentation/validation metadata and does not alter `documents`, `document_chunks`, stored PDF files, or stored DOCX files.
- Repository reads/writes preserve `evidence_text` across session reload.
